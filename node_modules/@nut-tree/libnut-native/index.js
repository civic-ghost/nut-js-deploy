const path = require('path');
const platform = process.platform;

// Load the appropriate binary based on platform
const binaryPath = path.join(__dirname, 'bin', platform, 'libnut.node');
module.exports = require(binaryPath);